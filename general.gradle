apply plugin: 'java'
apply plugin: 'maven'

sourceCompatibility = 1.8
group = 'org.tutske.utils'
version = 'git describe --dirty'.execute ().text.trim ();

repositories {
	mavenLocal ()
	mavenCentral ()
}

task copyDeps (type: Copy) {
	from configurations.runtime
	into "${buildDir}/libs"
}
